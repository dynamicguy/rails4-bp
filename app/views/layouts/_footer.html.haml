.span12
  .footer
    %ul
      %li
        %a{:href => "#"} Home
      %li= link_to 'About', page_path('about')
      %li= link_to 'Privacy Policy', page_path('policies')
      %li= link_to 'Terms and Conditions', page_path('terms_and_conditions')
      %li= link_to 'Contact Us', page_path('contacts')
      %li= link_to 'FAQ', page_path('faq')

      %li
        Copyright @2013 RAAKIB.COM

/
  Javascripts
  \==================================================
/ Placed at the end of the document so the pages load faster
-#= javascript_include_tag '//ajax.googleapis.com/ajax/libs/jquery/1.9.0/jquery.min.js', :defer=> true, :async => true
= render 'layouts/confirm_dialog'
= javascript_include_tag "application"
= yield :scripts
%script{:type => 'text/javascript'}
  = yield :document_ready
