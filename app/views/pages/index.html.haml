- model_class = Page

.span3.leftsidebar= render 'sidebar'
.span9
  = render_breadcrumbs
  .well
    .navbar.navbar-heading
      .navbar-inner
        .container
          %span.brand= t '.title', :default => model_class.model_name.human.pluralize + " " + t('labels.listing')
          = link_to raw('<i class="icon-plus-sign"></i> ' + t('.new', :default => t("helpers.links.new"))), new_page_path, :class => 'btn btn-success btn-mini btn-plus pull-right'

    %table.table.table-striped.table-bordered
      %thead
        %tr
          %th{:width => 10} <input type="checkbox" class="toggle">
          %th= model_class.human_attribute_name(:title)
          %th= model_class.human_attribute_name(:content)
          %th= model_class.human_attribute_name(:parent_id)
          %th= model_class.human_attribute_name(:lft)
          %th= model_class.human_attribute_name(:rgt)
          %th= model_class.human_attribute_name(:depth)
          %th=t '.actions', :default => t("helpers.actions")
      %tbody
        - @pages.each do |page|
          %tr
            %td <input id="bulk_ids_#{page.id.to_s}" name="bulk_ids[]" type="checkbox" value="#{page.id.to_s}">
            %td= link_to page.title, page_path(page)
            %td= truncate(page.title)
            %td= page.parent_id
            %td= page.lft
            %td= page.rgt
            %td= page.depth
            %td
              = link_to t('.edit', :default => t("helpers.links.edit")), edit_page_path(page), :class => 'btn btn-mini'
              = link_to t('.destroy', :default => t("helpers.links.destroy")), page_path(page), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-mini btn-danger'

    = paginate @pages